server:
  port: 8001
spring:
  application:
    name: local-grpc-server
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/imooc?serverTimezone=UTC
    username: root
    password: root
grpc:
  server:
    port: 8002
mybatis:
  mapper-locations: classpath:mapping/*.xml
  type-aliases-package: com.lrbj.grpcserver.entity